<h1 align = "center">LIST OF PRODUCTS</h1>
<hr>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Avatar</th>
      <th>Resume</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>

	<td><%= image_tag product.avatar.url(:polarized), :size => "150x100" %></td>
		
	<td><%= image_tag product.resume.url(:polarized), :size => "150x100" %></td>
		
        <!--<td><%= product.resume_file_name %></td>-->

        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<%= link_to 'New Product', new_product_path %>


      


